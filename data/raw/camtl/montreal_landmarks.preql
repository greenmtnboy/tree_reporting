import ..landmark_common;

root datasource montreal_landmarks_update_time (
    data_updated_through: camtl_landmark_data_updated_through
)
file `./montreal_landmarks_probe.py`;

# Montreal's 160 entries in the Repertoire du patrimoine culturel du Quebec,
# the province's official register of designated heritage property -- the
# runbook's first preference.  Two resources on Donnees Quebec (classified by
# the minister, cited by the municipality), unioned and filtered to this
# municipality by montreal_landmarks.py, so no Nominatim geocoding, no committed
# CSV and no staging object.
#
# `category` is the designation itself ("Classement" or "Citation") rather than
# a building type; `property_type` carries the register's `usage`.
root partial datasource raw_montreal_landmark_info (
    landmark_id: landmark_id,
    city: city,
    name: name,
    geometry_raw: ?geometry_raw,
    address: ?address,
    category: ?category,
    property_type: ?property_type,
    designation_date: ?designation_date,
)
grain (landmark_id)
complete where city = 'CAMTL'
file `./montreal_landmarks.py`;

partial datasource montreal_landmark_info (
    landmark_id,
    city,
    name,
    ?geometry,
    ?latitude,
    ?longitude,
    ?address,
    ?category,
    ?property_type,
    ?designation_date,
    camtl_landmark_data_updated_through,
)
grain (landmark_id)
complete where city = 'CAMTL'
file f`https://storage.googleapis.com/trilogy_public_models/duckdb/landmarks/camtl_landmark_info_v{data_version}.parquet`:f`gcs://trilogy_public_models/duckdb/landmarks/camtl_landmark_info_v{data_version}.parquet`
freshness by camtl_landmark_data_updated_through;
