import usbos.boston_tree_info;

WHERE usbos_source ='ARNOLD_ARBORETUM'
SELECT
    usbos_source,
    latitude,
    longitude,
    # species,
    count(tree_id) -> num_trees,
order by num_trees desc
LIMIT 100
;