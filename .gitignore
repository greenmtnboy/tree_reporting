.env
.env.local
.env.*.local
.secrets/
__pycache__/
.venv
.tmp
.claude
node_modules/

dist/

playwright-report/
test-results/

*.pyc
.uv-cache/
.pytest_cache/
.ruff_cache/

*.tsbuildinfo

src/vite.run.log

data/raw/*.parquet

.vscode/
.vscode/settings.json

scripts/*.png

# Generated imagery/model artifacts. The training scaffold and its configs are
# committed; downloaded rasters, materialized chips, checkpoints, and run logs
# are not.
imagery_model/artifacts/
imagery_model/data/
imagery_model/outputs/
imagery_model/lightning_logs/
imagery_model/wandb/
imagery_model/**/*.ckpt
imagery_model/**/*.tif
imagery_model/**/*.tiff
imagery_model/**/*.vrt
# Canonical human-reviewed bundles live in arborary-world/training-data.
imagery_model/annotations/*
!imagery_model/annotations/README.md
# Staged source parquets live in GCS (see _ingest_shared.staging_url), not in
# git: the probes read the object Last-Modified as the freshness watermark, and
# git does not preserve mtime, so a committed copy made every fresh clone look
# like newly extracted data.
*_staging.parquet

# Upstream bug repros and handoffs. They are worked on locally and sent to the
# upstream project rather than carried in this repo's history.
upstream_repro/
